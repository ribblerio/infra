# Ribbler · Infra

Deployment artifacts for Ribbler.

- `docker-compose.dev.yml` — Postgres only, for local development.
- `docker-compose.yml` — full stack (postgres + core-module + control-panel), added in Phase 6.
