services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ribbler_dev
      POSTGRES_USER: ribbler
      POSTGRES_PASSWORD: ribbler
    ports:
      - "5433:5432"
    volumes:
      - pgdata-dev:/var/lib/postgresql/data

volumes:
  pgdata-dev:
